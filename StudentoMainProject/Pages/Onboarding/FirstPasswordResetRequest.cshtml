@page
@model SchoolGradebook.Pages.FirstPasswordResetRequest
@{
    ViewData["title"] = "Jsi pozván/a do Studenta";
}
<h1>Jsi pozván/a do Studenta 🎉</h1>
<p>Jsi jedním z prvních uživatelů naší aplikace, na webu můžeš <strong>zapisovat svoje známky a sdílet své výpisky nebo stránky z učebnic se spolužáky</strong>. Každý měsíc ale přidáváme na web nové funkce. Můžeš se těšit například na spojení Studento.cz s Google Classroom, podporu rozvrhu a další super funkce.</p>
@if (ViewData["status"] != null)
{
    <div class="alert alert-success mt-4">@ViewData["status"]</div>
}
<form method="post" class="col-lg-3 pt-2 pl-0">
    <div class="form-group">
        <label asp-for="Email" class="control-label">Zadej svůj školní email</label>
        <input asp-for="Email" type="email" class="form-control" value="@Model.Email" />
    </div>
    <div class="form-group">
        <input type="submit" value="Aktivovat účet" class="btn btn-primary" />
    </div>
</form>
<div asp-validation-summary="All" class="text-danger"></div>
